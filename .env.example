# ─────────────────────────────────────────────
# Veramix — Environment Variables Template
# Copy this file to .env.local and fill in values
# NEVER commit the actual .env / .env.local files
# ─────────────────────────────────────────────

# ── Database (Supabase PostgreSQL) ────────────
# Pooled connection — used by the app at runtime
DATABASE_URL="postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true"

# Direct connection — used by Prisma Migrate only
DIRECT_URL="postgresql://postgres.<project-ref>:<password>@aws-0-<region>.pooler.supabase.com:5432/postgres"

# ── NextAuth v5 ───────────────────────────────
# Generate a strong secret: openssl rand -hex 32
AUTH_SECRET="replace-with-a-long-random-secret"

# Full canonical URL of your production site (no trailing slash)
NEXTAUTH_URL="https://yourdomain.com"

# ── Supabase Storage ──────────────────────────
SUPABASE_URL="https://<project-ref>.supabase.co"

# Service-role key — keep this secret, server-side only
SUPABASE_SERVICE_ROLE_KEY="sb_secret_..."

# Optional — public anon key (read-only, safe to expose)
SUPABASE_ANON_KEY="sb_publishable_..."

# Name of the storage bucket created in Supabase
SUPABASE_STORAGE_BUCKET="product-images"

# Storage provider: "supabase" | "cloudinary"
IMAGE_STORAGE_PROVIDER="supabase"

# ── Cloudinary (optional — only if IMAGE_STORAGE_PROVIDER=cloudinary) ────
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
CLOUDINARY_FOLDER="veramix-products"
